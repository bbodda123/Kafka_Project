<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Kafka Messaging</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <h1>Kafka Message System</h1>
    <form action="/produce" method="POST">
        <input
        type="text"
        name="message"
        placeholder="Enter a message"
        required
        />
        <button type="submit">Send</button>
    </form>
    <div class="messages">
        <% messages.forEach(msg => { %>
        <p><%= msg.message %></p>
        <% }) %>
    </div>
</body>
</html>
